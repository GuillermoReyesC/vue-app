const app = Vue.createApp({
    setup() {

        const title = 'this is a title from vue3';
        const name = 'minirick';
        return {
            title,
            name,

        };

    }
})